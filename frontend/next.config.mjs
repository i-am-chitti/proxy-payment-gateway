/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    images: {
        domains: ['localhost', 'api-ppg.tgsubscription.com'],
    }
};

export default nextConfig;
